README.md
CLAUDE.md
TODO.md
LICENSE
docs/
.github/
.plans/

# Exclude Darwin-only scripts on Linux
# Note: patterns match chezmoi's computed *target* name, not the source
# filename — attribute prefixes (run_once_, dot_, executable_, etc.) and the
# .tmpl suffix are stripped before matching.
{{ if ne .chezmoi.os "darwin" }}
darwin-*
{{ end }}

# Exclude Linux-only scripts and files on Mac
{{ if eq .chezmoi.os "darwin" }}
10-install-yay.sh
enable-tailscale.sh
install-session-manager.sh
30-install-fisher-nvm.sh
setup-ssh-include.sh
ensure-directories.sh
20-install-packages.sh
systemd-reload.sh
doctor.sh
.local/bin/clip-*
.config/systemd/
{{ end }}
